*** TODO ***

Separate class WXApp from WXWindow.cpp into WXApp.cpp
Implement main(...) and add wx startup macros
Refactor WXWindow/WXCanvas/WXImage data sharing / gl sharing shitness
Add class Preferences
	- Load(), Save()
	- Subclass wxConfig?
Add right click context menu
Drop dependency GLUT (AFTER wxWindow codepath fully supported on Windows AND Linux!)
Refactor away from old multi-backend design


Project:
	Get precompiled headers working
	Platform dependant clean (JFVGL vs JFVGL.exe etc)
	Compile wxWidgets from source
		- Strip and shrink resulting binaries
	Fix Linux linking shitness
		- Need to link wxWidgets on Linux...

View:
	Zoom around mouse cursor
	Keyboard input (esc etc) DONE PROPERLY
	Set appropriate default scale on load
		- IF SMALLER THAN SCREEN SIZE : zoom 100%, window to image size
		- IF NOT SMALLER THAN SCREEN SIZE : window to screen size, zoom to fit
	View individual channel

Image handling:
	Animated gif support

Shell integration:
	Add folder navigation
	Add drag / drop
